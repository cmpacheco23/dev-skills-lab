<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>
<body>
  <h1> Edit Skill:<%= skill.text %></h1>
  <form action="/skills/<%= skill._id %>?_method=PUT" method="POST" autocomplete="off">
    <input type="text" name="text" value="<%= skill.text %>">
    <input type="checkbox" <%= skill.learned  ? 'learned' : 'not yet learned'%> name="learned">
    <button type="submit">Save Skill</button>
  </form>
</body>
</html>